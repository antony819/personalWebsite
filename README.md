# Chan Ming Tung Antony — Resume Website

A professional, responsive resume and portfolio website for Chan Ming Tung Antony, Application Developer and full-stack problem solver.

## Customize Before Publishing

The main content has already been filled with Antony's experience, education, projects, skills, email, LinkedIn, and GitHub links.

Optional final touches:

- Save the attached profile photo as `profile.jpg` in this folder so it appears in the profile card.
- Add your real PDF resume as `resume.pdf` so the **Download Resume** button works.
- Review `index.html` if you want to refine wording, dates, job descriptions, or project details.

## Files

- `index.html` — resume and portfolio content.
- `styles.css` — modern responsive design.
- `script.js` — footer year and scroll reveal animations.

## Publish to GitHub Pages

1. Create a new GitHub repository named `portfolio`.
2. In this folder, commit and push your site:

```bash
git init
git add .
git commit -m "Create professional resume website"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/portfolio.git
git push -u origin main
```

3. On GitHub, open the repository.
4. Go to **Settings → Pages**.
5. Under **Build and deployment**, choose:
   - Source: **Deploy from a branch**
   - Branch: **main**
   - Folder: **/ (root)**
6. Save. Your site will be published at:

```text
https://YOUR_USERNAME.github.io/portfolio/
```

If you name the repository `YOUR_USERNAME.github.io`, GitHub will publish it at:

```text
https://YOUR_USERNAME.github.io/
```
